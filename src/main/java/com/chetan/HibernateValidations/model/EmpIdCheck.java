package com.chetan.HibernateValidations.model;
//We can assign groups to any validation, this can be useful to validate a
// set of fields only. For example, if I just need to validate the Employee id field,
// I can use EmpIdCheck group. For this we need to define a class/interface.
public interface EmpIdCheck {

}
